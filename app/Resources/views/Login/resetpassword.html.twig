{% extends "::layout.html.twig" %}

{% block title %}{{ parent() }} - Réinitialisation du mot de passe{% endblock %}

{% block body %}
	<div class="well">
	
		{% include "MessageFlash/messageFlash.html.twig" %}
		 {% if user and code %}
			<form action="{{ path("login_updatepassword", {}) }}" method="POST" >
				<div class="form-group">
				    <label for="inputPasswordFirst">Saisissez votre nouveau mot de passe :</label>
				    <input type="password" required="required" class="form-control" name="inputPasswordFirst" id="inputPasswordFirst">
				</div>
				
				<div class="form-group">
				    <label for="inputPasswordSecond">Resaisissez votre nouveau mot de passe :</label>
				    <input type="password" required="required" class="form-control" name="inputPasswordSecond" id="inputPasswordSecond">
				</div>
				
				<input type="hidden" value="{{ code }}" name="inputCode" id="inputCode">
				<input type="hidden" value="{{ user }}" name="inputUser" id="inputUser">
				
				<button type="submit" class="btn btn btn-primary">
			        Envoyer
			    </button>
			</form>
		{% endif %}
	</div>
{% endblock %}