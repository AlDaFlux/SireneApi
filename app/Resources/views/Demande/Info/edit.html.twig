{% extends 'base.html.twig' %}

{% block body %}
    <h1>Demande Infos {{application_name}}</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('demande_info_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
