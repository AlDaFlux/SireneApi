{# empty Twig template #}

{% set path = app.request.server.get("REQUEST_URI") %}

  
    {% if path starts with '/synthese/sauvegardes' %}
        href="{{ path('pericles3_sauvegardes_critere', {'id': critere_to_link_id}) }}#domaine_transversal"
    {% elseif path starts with '/synthese' %}
       href="{{ path('pericles3_synthese_critere', {'id': critere_to_link_id}) }}#critere_transversal"
     {% elseif path starts with '/eval' %}
        href="{{ path('pericles3_critere', {'id': critere_to_link_id}) }}"    
    {% endif %}

                