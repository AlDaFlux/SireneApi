{# empty Twig template #}

{% set path = app.request.server.get("REQUEST_URI") %}

  
                {% if path starts with '/eval' %}
                   href="{{ path('pericles3_domaine', {'id': domaine_to_link_id}) }}#domaine_transversal"
                {% elseif path starts with '/paq/osa/new/' %}
                   href="{{ path('paq_osa_new_domaine', {'id': domaine_to_link_id}) }}#domaine_transversal"
                {% elseif path starts with '/paq/osa' %}
                   href="{{ path('paq_osa_list_filtre_domaine', {'id': domaine_to_link_id}) }}#domaine_transversal"
                {% elseif path starts with '/paq/ooa' %}
                   href="{{ path('paq_ooa_list_filtre_domaine', {'id': domaine_to_link_id}) }}#domaine_transversal"
                {% elseif path starts with '/paq' %}
                   href="{{ path('pericles3_paq_organigramme_domaine', {'id': domaine_to_link_id}) }}#domaine_transversal"
                {% elseif path starts with '/synthese/sauvegardes' %}
                   href="{{ path('pericles3_sauvegardes_domaine', {'id': domaine_to_link_id}) }}#domaine_transversal"
                {% elseif path starts with '/synthese' %}
                   href="{{ path('pericles3_synthese_domaine', {'id': domaine_to_link_id}) }}#domaine_transversal"
                {% elseif path starts with '/bibliotheque' %}
                   href="{{ path('pericles3_bibliotheque_preuves_domaine', {'id': domaine_to_link_id}) }}#domaine_transversal"
                {% endif %}

                
                
                