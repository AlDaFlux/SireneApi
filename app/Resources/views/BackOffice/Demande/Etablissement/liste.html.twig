{% extends "BackOffice/Demande/layout.html.twig" %}
{% block body %}{{ parent() }}
    <h2>
        Etablissement
    </h2>


    {% if targetCreai is not empty  %}
        <h3>{{targetCreai }}</h3>
    {% endif  %}
    
        {% include "BackOffice/Demande/Etablissement/inc_liste.html.twig" %}

        <p>
            
    {% if app.user.isCreai %}
        <a class='btn btn-link full-width ' href="{{ path('demande_etablissement_new') }}">Nouvelle demande de création d'établissement</a>
    {% endif  %}

{% endblock %}
