[{% for gestionnaire in results -%}
    {{ {id: gestionnaire.id, label: gestionnaire.nom , value: gestionnaire.nom  }|json_encode|raw }}
    {# use "value" instead of "id" key, if you use jquery-ui #}
    {%- if not loop.last %},{% endif -%}
{%- endfor %}]

